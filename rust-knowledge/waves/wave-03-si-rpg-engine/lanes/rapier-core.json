{
  "laneSlug": "rapier-core",
  "title": "Rapier 0.35: pipeline, determinism & upgrades",
  "tier": "si-rpg-engine",
  "recipes": [
    {
      "name": "Call PhysicsPipeline::step at 0.35.3 with its 12 arguments and read contacts as start-of-step state",
      "what": "One 0.35.3 step applies queued user changes, runs broad and narrow phase once, solves and integrates per CCD substep, then only refreshes broad-phase AABBs; a non-finite body is quarantined, not reported as an error.",
      "how": "Signature (docs.rs 0.35.3): step(&mut self, gravity: Vector, integration_parameters: &IntegrationParameters, islands: &mut IslandManager, broad_phase: &mut BroadPhaseBvh, narrow_phase: &mut NarrowPhase, bodies: &mut RigidBodySet, colliders: &mut ColliderSet, impulse_joints: &mut ImpulseJointSet, multibody_joints: &mut MultibodyJointSet, ccd_solver: &mut CCDSolver, hooks: &dyn PhysicsHooks, events: &dyn EventHandler). Gravity is a glam Vector by value, the broad phase is the concrete BroadPhaseBvh (alias DefaultBroadPhase), `&()` implements both PhysicsHooks and EventHandler, and PhysicsWorld::step() is step_with_events(&(), &()) over the world's own fields. Order in step_inner (substep.rs): reset counters and the quarantine report; drain joint wake-ups and joins; quarantine non-finite user changes; apply user changes to colliders, then bodies (new bodies join the awake island here); detect_collisions = BroadPhaseBvh::update, then the narrow phase's handle_user_changes, register_pairs, compute_contacts, compute_intersections (filter hooks and collision events run here); clear change flags; then per CCD substep (max_ccd_substeps == 0 means one substep and no CCD): kinematic position-based velocities from their next pose, island update and staged velocity solve with gravity, contact-force events, CCD motion clamping, advance_to_final_positions (pose := next pose, collider poses, fresh AABBs), quarantine; after the last substep only BroadPhaseBvh::set_aabb for moved colliders, no narrow phase. The staged solver runs only for the awake island. The step copies IntegrationParameters and rewrites dt only when CCD splits the step; it mutates the islands, both phases, the bodies and colliders, the joint sets' pending wake-ups and joins, and the CCD solver when CCD runs.",
      "rust_version": "rapier3d-f64 0.35.3",
      "gotchas": "The rapier.rs guide documents 0.36 and its step call passes a 13th argument (&mut soft_body_set): pasted into a 0.35.3 crate it fails with E0433 (no SoftBodySet) and E0061 (12 vs 13 arguments). Older tutorials pass &gravity, a generic broad phase or a QueryPipeline argument; none of those exist at 0.35.3. Because the narrow phase runs before integration, contact_pairs() read after a step describe the poses at the start of that step while scene queries see end-of-step AABBs: measured, a box leaving a wall at 20 u/s is 0.3125 clear after one step yet still has an active contact, and a ray already sees its new face. Non-finite state never panics: since 0.35.0-beta.0 the Quarantine disables the body, keeps its last valid pose, zeroes its velocity and lists it in quarantine() for that step only; measured, an infinite velocity passes an is_nan() guard and leaves an all-finite, disabled body hanging at y = 2 under gravity.",
      "engine_note": "rapier_law.rs `integrate` calls `loaded.world.step()` (PhysicsWorld: no hooks, no events) after set_next_kinematic_translation and solve_character_collision_impulses, and `rebuild_snapshot` then hashes contact pairs computed before that quantum's integration. `bad()` tests is_nan() only, so an infinite velocity in a body record passes `signature`, reaches Rapier, is quarantined and is written back finite by `write_body`: refuse `!x.is_finite()` and `!loaded.world.quarantine().is_empty()` right after `step()` so that 'NaN aborts a step' also covers what Rapier absorbs.",
      "checks": [
        {
          "label": "the 0.35.3 step coerces to a 12-parameter fn pointer; &() serves as hooks and events",
          "edition": "2021",
          "target": "host",
          "deps": [
            "rapier3d_f64"
          ],
          "expect": "compiles",
          "crate_type": "lib",
          "source": "use rapier3d_f64::prelude::*;\n\n// The 0.35.3 step as a function pointer: gravity by value, the concrete BroadPhaseBvh,\n// hooks and events as trait objects, and no SoftBodySet.\npub type Step = fn(\n    &mut PhysicsPipeline,\n    Vector,\n    &IntegrationParameters,\n    &mut IslandManager,\n    &mut BroadPhaseBvh,\n    &mut NarrowPhase,\n    &mut RigidBodySet,\n    &mut ColliderSet,\n    &mut ImpulseJointSet,\n    &mut MultibodyJointSet,\n    &mut CCDSolver,\n    &dyn PhysicsHooks,\n    &dyn EventHandler,\n);\n\npub const STEP: Step = PhysicsPipeline::step;\n\n// `&()` satisfies both trait-object parameters (what PhysicsWorld::step passes).\npub fn no_hooks_no_events() -> (&'static dyn PhysicsHooks, &'static dyn EventHandler) {\n    (&(), &())\n}\n"
        },
        {
          "label": "the rapier.rs (Rust 0.36) guide's 13-argument step call does not compile against 0.35.3",
          "edition": "2021",
          "target": "host",
          "deps": [
            "rapier3d_f64"
          ],
          "expect": "compile_fail",
          "crate_type": "lib",
          "error_codes": [
            "E0433",
            "E0061"
          ],
          "stderr_contains": [
            "this method takes 12 arguments but 13 arguments were supplied"
          ],
          "source": "use rapier3d_f64::prelude::*;\n\n// The step call as printed by the rapier.rs user guide, which documents Rust 0.36.\npub fn step_like_the_guide() {\n    let gravity = Vector::new(0.0, -9.81, 0.0);\n    let integration_parameters = IntegrationParameters::default();\n    let mut physics_pipeline = PhysicsPipeline::new();\n    let mut island_manager = IslandManager::new();\n    let mut broad_phase = DefaultBroadPhase::new();\n    let mut narrow_phase = NarrowPhase::new();\n    let mut rigid_body_set = RigidBodySet::new();\n    let mut collider_set = ColliderSet::new();\n    let mut impulse_joint_set = ImpulseJointSet::new();\n    let mut multibody_joint_set = MultibodyJointSet::new();\n    let mut soft_body_set = SoftBodySet::new();\n    let mut ccd_solver = CCDSolver::new();\n    let physics_hooks = ();\n    let event_handler = ();\n    physics_pipeline.step(\n        gravity,\n        &integration_parameters,\n        &mut island_manager,\n        &mut broad_phase,\n        &mut narrow_phase,\n        &mut rigid_body_set,\n        &mut collider_set,\n        &mut impulse_joint_set,\n        &mut multibody_joint_set,\n        &mut soft_body_set,\n        &mut ccd_solver,\n        &physics_hooks,\n        &event_handler,\n    );\n}\n"
        },
        {
          "label": "contacts after a step are start-of-step; the query BVH already has end-of-step poses",
          "edition": "2021",
          "target": "host",
          "deps": [
            "rapier3d_f64"
          ],
          "expect": "runs",
          "crate_type": "bin",
          "stdout": "after step 1: gap=0.3125 contact_active=true ray_toi=1.0875\nafter step 2: gap=0.6250 contact_active=false ray_toi=0.7750",
          "source": "use rapier3d_f64::prelude::*;\n\nfn main() {\n    let mut w = PhysicsWorld::new();\n    w.gravity = Vector::ZERO;\n    w.integration_parameters.dt = 1.0 / 64.0;\n    let (_, wall) = w.insert(RigidBodyBuilder::fixed(), ColliderBuilder::cuboid(0.1, 1.0, 1.0));\n    // Box face flush with the wall face, leaving at 20 units per second.\n    let (h, bx) = w.insert(\n        RigidBodyBuilder::dynamic()\n            .translation(Vector::new(0.35, 0.0, 0.0))\n            .linvel(Vector::new(20.0, 0.0, 0.0)),\n        ColliderBuilder::cuboid(0.25, 0.25, 0.25),\n    );\n    for step in 1..=2 {\n        w.step();\n        let gap = w.bodies[h].translation().x - 0.25 - 0.1;\n        let contact = w\n            .narrow_phase\n            .contact_pair(wall, bx)\n            .is_some_and(|p| p.has_any_active_contact());\n        // A ray from x = 2 back towards the wall meets the box's +x face.\n        let ray = Ray::new(Vector::new(2.0, 0.0, 0.0), Vector::new(-1.0, 0.0, 0.0));\n        let toi = w.query_pipeline().cast_ray(&ray, 10.0, false).map(|(_, t)| t).unwrap_or(-1.0);\n        println!(\"after step {step}: gap={gap:.4} contact_active={contact} ray_toi={toi:.4}\");\n    }\n}\n"
        },
        {
          "label": "an infinite velocity is quarantined: body disabled, state finite, report gone next step",
          "edition": "2021",
          "target": "host",
          "deps": [
            "rapier3d_f64"
          ],
          "expect": "runs",
          "crate_type": "bin",
          "stdout": "is_nan guard passes: true\nquarantined=1 enabled=false state_finite=true y=2 vx=0\nnext step: reports=0 y=2",
          "source": "use rapier3d_f64::prelude::*;\n\nfn main() {\n    let mut w = PhysicsWorld::new();\n    w.integration_parameters.dt = 1.0 / 64.0;\n    // An infinite velocity passes an `is_nan()` guard.\n    let v = Vector::new(f64::INFINITY, 0.0, 0.0);\n    println!(\"is_nan guard passes: {}\", !v.x.is_nan());\n    let (h, _) = w.insert(\n        RigidBodyBuilder::dynamic().translation(Vector::new(0.0, 2.0, 0.0)).linvel(v),\n        ColliderBuilder::ball(0.5),\n    );\n    w.step();\n    let b = &w.bodies[h];\n    let (p, u) = (b.translation(), b.linvel());\n    let finite = [p.x, p.y, p.z, u.x, u.y, u.z].iter().all(|x| x.is_finite());\n    println!(\n        \"quarantined={} enabled={} state_finite={} y={} vx={}\",\n        w.quarantine().bodies().len(),\n        b.is_enabled(),\n        finite,\n        p.y,\n        u.x\n    );\n    w.step();\n    println!(\"next step: reports={} y={}\", w.quarantine().bodies().len(), w.bodies[h].translation().y);\n}\n"
        }
      ],
      "currency": null,
      "verify_note": null,
      "sources": [
        {
          "title": "PhysicsPipeline in rapier3d_f64::pipeline (0.35.3)",
          "url": "https://docs.rs/rapier3d-f64/0.35.3/rapier3d_f64/pipeline/struct.PhysicsPipeline.html",
          "year": 2026,
          "kind": "docs",
          "identifier": "",
          "claim": "At 0.35.3 step takes gravity: Vector by value, &IntegrationParameters, &mut IslandManager, &mut BroadPhaseBvh, &mut NarrowPhase, the four sets, &mut CCDSolver, &dyn PhysicsHooks and &dyn EventHandler, and the pipeline exposes quarantine().",
          "retrieved": true
        },
        {
          "title": "rapier3d-f64 0.35.3 source: src/pipeline/physics_pipeline/substep.rs",
          "url": "https://docs.rs/crate/rapier3d-f64/0.35.3/source/src/pipeline/physics_pipeline/substep.rs",
          "year": 2026,
          "kind": "repo",
          "identifier": "",
          "claim": "step_inner applies user changes, runs detect_collisions, then per CCD substep solves, clamps and advances, and after the last substep only refreshes the broad-phase BVH; max_ccd_substeps == 0 disables CCD.",
          "retrieved": true
        },
        {
          "title": "rapier3d-f64 0.35.3 source: src/pipeline/physics_pipeline/solve.rs",
          "url": "https://docs.rs/crate/rapier3d-f64/0.35.3/source/src/pipeline/physics_pipeline/solve.rs",
          "year": 2026,
          "kind": "repo",
          "identifier": "",
          "claim": "detect_collisions runs BroadPhaseBvh::update, then handle_user_changes, register_pairs, compute_contacts and compute_intersections (the last two get hooks and events); the solve applies gravity through compute_effective_force_and_torque, runs the staged solver only for the awake island and then emits contact-force events.",
          "retrieved": true
        },
        {
          "title": "Simulation structures (User Guide: Rust 0.36)",
          "url": "https://rapier.rs/docs/user_guides/rust/simulation_structures/",
          "year": 2026,
          "kind": "docs",
          "identifier": "",
          "claim": "The current rapier.rs guide is labelled Rust 0.36 and its PhysicsPipeline::step example passes &mut soft_body_set between the multibody joints and the CCD solver.",
          "retrieved": true
        },
        {
          "title": "Rapier CHANGELOG at tag v0.36.0",
          "url": "https://github.com/dimforge/rapier/blob/v0.36.0/CHANGELOG.md",
          "year": 2026,
          "kind": "release-notes",
          "identifier": "",
          "claim": "0.36.0 makes PhysicsPipeline::step take the SoftBodySet; 0.35.0-beta.0 added the NaN/infinity quarantine that rolls back, disables and reports non-finite bodies.",
          "retrieved": true
        }
      ],
      "background_sources": []
    },
    {
      "name": "Read rapier3d-f64 0.35.3 IntegrationParameters defaults from code: prediction 0.02, slop 0.005, CCD on",
      "what": "The 0.35.3 defaults that decide results are prediction 0.02, allowed error 0.005, 4 solver iterations, 30 Hz / ζ 10 contacts (60 Hz against fixed bodies), CCD and contact recycling on, and two doc comments still state the pre-0.35 values.",
      "how": "Fields as named in 0.35.3, defaults printed by the compiler: dt 1/60; min_ccd_dt 1/6000; num_solver_iterations 4; num_internal_pgs_iterations 1; num_internal_stabilization_iterations 1; warmstart_coefficient 1; warmstart_joints false; length_unit 1; contact_softness = SpringCoefficients { natural_frequency: 30, damping_ratio: 10 }; static_contact_softness = { 60, 10 }; normalized_allowed_linear_error 0.005; normalized_prediction_distance 0.02; normalized_max_corrective_velocity 3; normalized_max_linear_velocity 400; max_ccd_substeps 1; contact_clustering true; contact_recycling true; normalized_contact_recycle_distance 0.05; friction_in_bias_pass false; friction_model Simplified. The normalized_* values are multiplied by length_unit through allowed_linear_error(), prediction_distance(), max_corrective_velocity(), max_linear_velocity() and contact_recycle_distance(). max_ccd_substeps is the world's CCD switch: 0 turns off every sweep, including the automatic one of fast dynamic bodies against fixed colliders that runs even with ccd_enabled(false) (measured: a 20 u/s box stops in front of a thin slab at the default, passes it at 0). Set every field the law depends on in one place and record them beside the goldens.",
      "rust_version": "rapier3d-f64 0.35.3",
      "gotchas": "docs.rs 0.35.3 says normalized_prediction_distance defaults to 0.002m and allowed_linear_error() to 0.001 × length_unit; the Default impl, the 0.36 guide and the 0.35.0-beta.0 changelog say 0.02 and 0.005 (the changelog: 0.002 → 0.02, 10.0 → 3.0, 0.001 → 0.005). Fields removed earlier fail with E0609: min_island_size (0.35.0-beta.0) and contact_natural_frequency (grouped into contact_softness in 0.31, with the erp/cfm methods). contact_recycling changes result bits (measured on a four-box pile after 200 steps) and keeps per-pair state from the last full narrow-phase update; contact_clustering left the same pile bit-identical (a box-box pair emits one manifold; the field doc names meshes, heightfields, compounds and voxels as the shapes that emit several). warmstart_coefficient = 1.0 is already the default.",
      "engine_note": "rapier_law.rs `build_world` sets dt = 1/64, contact_clustering = false and warmstart_coefficient = 1.0 (a no-op) and inherits the rest, so the law runs with contact_recycling = true and max_ccd_substeps = 1: dynamic bodies built with ccd_enabled(false) are still swept against fixed slabs and the heightfield. T4 pin 2 assumes the thin fast body 'passes through' today; measured here, a 0.05-half box at 20 u/s with ccd_enabled(false) stops at x 0.921 in front of a 0.02-half slab. Measure the exact T4 scene before moving goldens (the tunnelling numbers belong to binary-and-limits); the recycled pair state is outside the snapshot, a T2 question for restore-internals.",
      "checks": [
        {
          "label": "IntegrationParameters::default() at 0.35.3: prediction 0.02, allowed error 0.005, CCD 1",
          "edition": "2021",
          "target": "host",
          "deps": [
            "rapier3d_f64"
          ],
          "expect": "runs",
          "crate_type": "bin",
          "stdout": "dt=0.016666666666666666 min_ccd_dt=0.00016666666666666666\nnum_solver_iterations=4 num_internal_pgs_iterations=1 num_internal_stabilization_iterations=1\nwarmstart_coefficient=1 warmstart_joints=false length_unit=1\ncontact_softness=30Hz/10 static_contact_softness=60Hz/10\nnormalized_allowed_linear_error=0.005 allowed_linear_error()=0.005\nnormalized_prediction_distance=0.02 prediction_distance()=0.02\nnormalized_max_corrective_velocity=3 normalized_max_linear_velocity=400\nmax_ccd_substeps=1 contact_clustering=true contact_recycling=true normalized_contact_recycle_distance=0.05\nfriction_in_bias_pass=false friction_model=Simplified",
          "source": "use rapier3d_f64::prelude::*;\n\nfn main() {\n    let p = IntegrationParameters::default();\n    println!(\"dt={} min_ccd_dt={}\", p.dt, p.min_ccd_dt);\n    println!(\n        \"num_solver_iterations={} num_internal_pgs_iterations={} num_internal_stabilization_iterations={}\",\n        p.num_solver_iterations, p.num_internal_pgs_iterations, p.num_internal_stabilization_iterations\n    );\n    println!(\n        \"warmstart_coefficient={} warmstart_joints={} length_unit={}\",\n        p.warmstart_coefficient, p.warmstart_joints, p.length_unit\n    );\n    println!(\n        \"contact_softness={}Hz/{} static_contact_softness={}Hz/{}\",\n        p.contact_softness.natural_frequency,\n        p.contact_softness.damping_ratio,\n        p.static_contact_softness.natural_frequency,\n        p.static_contact_softness.damping_ratio\n    );\n    println!(\n        \"normalized_allowed_linear_error={} allowed_linear_error()={}\",\n        p.normalized_allowed_linear_error,\n        p.allowed_linear_error()\n    );\n    println!(\n        \"normalized_prediction_distance={} prediction_distance()={}\",\n        p.normalized_prediction_distance,\n        p.prediction_distance()\n    );\n    println!(\n        \"normalized_max_corrective_velocity={} normalized_max_linear_velocity={}\",\n        p.normalized_max_corrective_velocity, p.normalized_max_linear_velocity\n    );\n    println!(\n        \"max_ccd_substeps={} contact_clustering={} contact_recycling={} normalized_contact_recycle_distance={}\",\n        p.max_ccd_substeps, p.contact_clustering, p.contact_recycling, p.normalized_contact_recycle_distance\n    );\n    println!(\"friction_in_bias_pass={} friction_model={:?}\", p.friction_in_bias_pass, p.friction_model);\n}\n"
        },
        {
          "label": "fields older tutorials set (min_island_size, contact_natural_frequency) are gone: E0609",
          "edition": "2021",
          "target": "host",
          "deps": [
            "rapier3d_f64"
          ],
          "expect": "compile_fail",
          "crate_type": "lib",
          "error_codes": [
            "E0609"
          ],
          "source": "use rapier3d_f64::prelude::*;\n\n// Two fields older tutorials set. Neither exists at 0.35.3.\npub fn tune(p: &mut IntegrationParameters) {\n    p.min_island_size = 128; // removed in 0.35.0-beta.0 (single active set)\n    p.contact_natural_frequency = 30.0; // grouped into contact_softness in 0.31\n}\n"
        },
        {
          "label": "ccd_enabled(false) is still swept against a fixed slab; max_ccd_substeps = 0 turns CCD off",
          "edition": "2021",
          "target": "host",
          "deps": [
            "rapier3d_f64"
          ],
          "expect": "runs",
          "crate_type": "bin",
          "stdout": "max_ccd_substeps=1 ccd_enabled=false: x=0.921\nmax_ccd_substeps=0 ccd_enabled=false: x=4.900\nmax_ccd_substeps=0 ccd_enabled=true: x=4.900\nmax_ccd_substeps=1 ccd_enabled=true: x=0.921",
          "source": "use rapier3d_f64::prelude::*;\n\n// A 0.05-half box at 20 units/s (0.3125 per 1/64 s step) toward a fixed slab of\n// half-thickness 0.02 at x = 1. From x = -0.1 its positions jump the slab between steps.\nfn shoot(max_ccd_substeps: usize, ccd_enabled: bool) -> f64 {\n    let mut w = PhysicsWorld::new();\n    w.gravity = Vector::ZERO;\n    w.integration_parameters.dt = 1.0 / 64.0;\n    w.integration_parameters.max_ccd_substeps = max_ccd_substeps;\n    w.insert(\n        RigidBodyBuilder::fixed().translation(Vector::new(1.0, 0.0, 0.0)),\n        ColliderBuilder::cuboid(0.02, 2.0, 2.0),\n    );\n    let (h, _) = w.insert(\n        RigidBodyBuilder::dynamic()\n            .translation(Vector::new(-0.1, 0.0, 0.0))\n            .linvel(Vector::new(20.0, 0.0, 0.0))\n            .ccd_enabled(ccd_enabled),\n        ColliderBuilder::cuboid(0.05, 0.05, 0.05),\n    );\n    for _ in 0..16 {\n        w.step();\n    }\n    w.bodies[h].translation().x\n}\n\nfn main() {\n    for (substeps, ccd) in [(1, false), (0, false), (0, true), (1, true)] {\n        println!(\"max_ccd_substeps={} ccd_enabled={}: x={:.3}\", substeps, ccd, shoot(substeps, ccd));\n    }\n}\n"
        },
        {
          "label": "contact_recycling changes result bits on a box pile; contact_clustering does not there",
          "edition": "2021",
          "target": "host",
          "deps": [
            "rapier3d_f64"
          ],
          "expect": "runs",
          "crate_type": "bin",
          "stdout": "recycling on vs off identical: false\nclustering on vs off identical: true",
          "source": "use rapier3d_f64::prelude::*;\n\n// FNV-1a over the bit patterns of four boxes after 200 steps of a small pile.\nfn pile(recycling: bool, clustering: bool) -> u64 {\n    let starts = [\n        Vector::new(0.00, 0.40, 0.00),\n        Vector::new(0.13, 0.95, 0.07),\n        Vector::new(-0.11, 1.50, 0.05),\n        Vector::new(0.05, 2.05, -0.12),\n    ];\n    let mut w = PhysicsWorld::new();\n    w.gravity = Vector::new(0.0, -8.0, 0.0);\n    w.integration_parameters.dt = 1.0 / 64.0;\n    w.integration_parameters.contact_recycling = recycling;\n    w.integration_parameters.contact_clustering = clustering;\n    w.insert(RigidBodyBuilder::fixed(), ColliderBuilder::cuboid(5.0, 0.1, 5.0));\n    let hs: Vec<RigidBodyHandle> = starts\n        .iter()\n        .map(|s| {\n            w.insert(RigidBodyBuilder::dynamic().translation(*s), ColliderBuilder::cuboid(0.25, 0.25, 0.25)).0\n        })\n        .collect();\n    for _ in 0..200 {\n        w.step();\n    }\n    let mut h = 0xcbf29ce484222325u64;\n    for hd in &hs {\n        let b = &w.bodies[*hd];\n        let (p, q) = (b.translation(), b.rotation());\n        for x in [p.x, p.y, p.z, q.x, q.y, q.z, q.w] {\n            for byte in x.to_bits().to_le_bytes() {\n                h = (h ^ byte as u64).wrapping_mul(0x100000001b3);\n            }\n        }\n    }\n    h\n}\n\nfn main() {\n    let base = pile(true, false);\n    println!(\"recycling on vs off identical: {}\", base == pile(false, false));\n    println!(\"clustering on vs off identical: {}\", base == pile(true, true));\n}\n"
        }
      ],
      "currency": null,
      "verify_note": null,
      "sources": [
        {
          "title": "IntegrationParameters in rapier3d_f64::dynamics (0.35.3)",
          "url": "https://docs.rs/rapier3d-f64/0.35.3/rapier3d_f64/dynamics/struct.IntegrationParameters.html",
          "year": 2026,
          "kind": "docs",
          "identifier": "",
          "claim": "Lists the 0.35.3 fields; its doc comments still give 0.002m for normalized_prediction_distance and 0.001 × length_unit for allowed_linear_error().",
          "retrieved": true
        },
        {
          "title": "Integration parameters (User Guide: Rust 0.36)",
          "url": "https://rapier.rs/docs/user_guides/rust/integration_parameters/",
          "year": 2026,
          "kind": "docs",
          "identifier": "",
          "claim": "The guide lists normalized_prediction_distance 0.02, normalized_allowed_linear_error 0.005, max corrective velocity 3.0, max linear velocity 400, max_ccd_substeps 1, contact_clustering and contact_recycling true.",
          "retrieved": true
        },
        {
          "title": "Rapier CHANGELOG at tag v0.35.3",
          "url": "https://github.com/dimforge/rapier/blob/v0.35.3/CHANGELOG.md",
          "year": 2026,
          "kind": "release-notes",
          "identifier": "",
          "claim": "0.35.0-beta.0 changed the contact defaults (prediction 0.002→0.02, corrective velocity 10→3, allowed error 0.001→0.005), removed min_island_size, made fast dynamic bodies always sweep fixed colliders and made max_ccd_substeps = 0 disable CCD; 0.31 grouped contact_natural_frequency into contact_softness.",
          "retrieved": true
        },
        {
          "title": "rapier3d-f64 0.35.3 source: src/pipeline/physics_pipeline/substep.rs",
          "url": "https://docs.rs/crate/rapier3d-f64/0.35.3/source/src/pipeline/physics_pipeline/substep.rs",
          "year": 2026,
          "kind": "repo",
          "identifier": "",
          "claim": "max_ccd_substeps == 0 yields (ccd_is_enabled = false, one substep); otherwise CCD motion clamping runs after each solve.",
          "retrieved": true
        }
      ],
      "background_sources": []
    },
    {
      "name": "Expect Rapier 0.35.3 body types to gate contacts: kinematic-fixed off, kinematic pushes dynamic one way",
      "what": "By default only pairs involving a dynamic body get contacts; kinematic bodies follow their next pose (position-based) or their velocity (velocity-based) and push dynamic bodies without being pushed; fixed bodies never move.",
      "how": "RigidBodyType: Dynamic = 0 (forces, gravity, contacts), Fixed = 1 (infinite mass, never moves), KinematicPositionBased = 2 (set_next_kinematic_translation / rotation; the step derives the velocity: measured 0.1 in one 1/64 s step gives vx 6.4), KinematicVelocityBased = 3 (set linvel / angvel; the pose integrates: 6.4 u/s gives x 0.1 after one step). ActiveCollisionTypes::default() = DYNAMIC_DYNAMIC | DYNAMIC_KINEMATIC | DYNAMIC_FIXED; OR in KINEMATIC_FIXED, KINEMATIC_KINEMATIC or FIXED_FIXED per collider when those pairs must produce contacts or events (measured: setting KINEMATIC_FIXED on the kinematic collider alone was enough). A kinematic body's trajectory ignores what it hits: measured, 64 steps at 2 u/s end at exactly x = 2 while pushing a box ahead of it.",
      "rust_version": "rapier3d-f64 0.35.3",
      "gotchas": "A kinematic body overlapping a fixed wall has no active contact by default, so contacts never tell a kinematic character that it is blocked: the character controller's shape casts do. Enabling KINEMATIC_FIXED creates contacts and events, not a push-back. Kinematic bodies sleep only when both velocities are exactly zero (0.30.1).",
      "engine_note": "rapier_law.rs `build_world` makes driven bodies kinematic_position_based() with lock_rotations, can_sleep(false), ccd_enabled(false) and additional_mass(1.0), and slabs and the heightfield fixed(); with default ActiveCollisionTypes the walker has active contacts only with dynamic boxes, and `integrate` resolves walls with KinematicCharacterController::move_shape (shape casts, not contacts) and pushes boxes with solve_character_collision_impulses. Two walkers are kinematic-kinematic and get no contacts either, so T4's 'character against a character' case is decided by the controller's casts alone.",
      "checks": [
        {
          "label": "kinematic-fixed contacts need KINEMATIC_FIXED; both kinematic kinds move; kinematic is not pushed",
          "edition": "2021",
          "target": "host",
          "deps": [
            "rapier3d_f64"
          ],
          "expect": "runs",
          "crate_type": "bin",
          "stdout": "kinematic-fixed default: false\nkinematic-fixed with KINEMATIC_FIXED: true\nposition-based: x=0.1 vx=6.4\nvelocity-based: x=0.1 vx=6.4\nkinematic after 64 steps: x=2 vx=2; box pushed ahead by more than 0.99: true",
          "source": "use rapier3d_f64::prelude::*;\n\nfn kinematic_vs_fixed(types: ActiveCollisionTypes) -> bool {\n    let mut w = PhysicsWorld::new();\n    w.gravity = Vector::ZERO;\n    w.integration_parameters.dt = 1.0 / 64.0;\n    let (_, wall) = w.insert(\n        RigidBodyBuilder::fixed().translation(Vector::new(0.9, 0.0, 0.0)),\n        ColliderBuilder::cuboid(0.5, 0.5, 0.5),\n    );\n    // Overlapping the wall by 0.1.\n    let (_, kc) = w.insert(\n        RigidBodyBuilder::kinematic_position_based(),\n        ColliderBuilder::cuboid(0.5, 0.5, 0.5).active_collision_types(types),\n    );\n    for _ in 0..2 {\n        w.step();\n    }\n    w.narrow_phase.contact_pair(wall, kc).is_some_and(|p| p.has_any_active_contact())\n}\n\nfn main() {\n    println!(\"kinematic-fixed default: {}\", kinematic_vs_fixed(ActiveCollisionTypes::default()));\n    println!(\n        \"kinematic-fixed with KINEMATIC_FIXED: {}\",\n        kinematic_vs_fixed(ActiveCollisionTypes::default() | ActiveCollisionTypes::KINEMATIC_FIXED)\n    );\n\n    // Position-based: velocity derived from the next pose. Velocity-based: pose from velocity.\n    let mut w = PhysicsWorld::new();\n    w.gravity = Vector::ZERO;\n    w.integration_parameters.dt = 1.0 / 64.0;\n    let (kp, _) = w.insert(RigidBodyBuilder::kinematic_position_based(), ColliderBuilder::cuboid(0.5, 0.5, 0.5));\n    let (kv, _) = w.insert(\n        RigidBodyBuilder::kinematic_velocity_based()\n            .translation(Vector::new(0.0, 5.0, 0.0))\n            .linvel(Vector::new(6.4, 0.0, 0.0)),\n        ColliderBuilder::cuboid(0.5, 0.5, 0.5),\n    );\n    w.bodies[kp].set_next_kinematic_translation(Vector::new(0.1, 0.0, 0.0));\n    w.step();\n    println!(\"position-based: x={} vx={}\", w.bodies[kp].translation().x, w.bodies[kp].linvel().x);\n    println!(\"velocity-based: x={} vx={}\", w.bodies[kv].translation().x, w.bodies[kv].linvel().x);\n\n    // A kinematic body pushes a dynamic box and is not pushed back.\n    let mut w = PhysicsWorld::new();\n    w.gravity = Vector::ZERO;\n    w.integration_parameters.dt = 1.0 / 64.0;\n    let (k, _) = w.insert(\n        RigidBodyBuilder::kinematic_velocity_based().linvel(Vector::new(2.0, 0.0, 0.0)),\n        ColliderBuilder::cuboid(0.5, 0.5, 0.5),\n    );\n    let (d, _) = w.insert(\n        RigidBodyBuilder::dynamic().translation(Vector::new(1.2, 0.0, 0.0)),\n        ColliderBuilder::cuboid(0.5, 0.5, 0.5),\n    );\n    for _ in 0..64 {\n        w.step();\n    }\n    let kx = w.bodies[k].translation().x;\n    println!(\n        \"kinematic after 64 steps: x={} vx={}; box pushed ahead by more than 0.99: {}\",\n        kx,\n        w.bodies[k].linvel().x,\n        w.bodies[d].translation().x > kx + 0.99\n    );\n}\n"
        }
      ],
      "currency": null,
      "verify_note": null,
      "sources": [
        {
          "title": "RigidBodyType in rapier3d_f64::dynamics (0.35.3)",
          "url": "https://docs.rs/rapier3d-f64/0.35.3/rapier3d_f64/dynamics/enum.RigidBodyType.html",
          "year": 2026,
          "kind": "docs",
          "identifier": "",
          "claim": "Dynamic = 0 is fully simulated, Fixed = 1 never moves, KinematicPositionBased = 2 derives its velocity from the next position and KinematicVelocityBased = 3 is driven by velocity; both kinematic types push dynamic bodies and are not pushed back.",
          "retrieved": true
        },
        {
          "title": "ActiveCollisionTypes in rapier3d_f64::geometry (0.35.3)",
          "url": "https://docs.rs/rapier3d-f64/0.35.3/rapier3d_f64/geometry/struct.ActiveCollisionTypes.html",
          "year": 2026,
          "kind": "docs",
          "identifier": "",
          "claim": "By default dynamic-dynamic, dynamic-fixed and dynamic-kinematic pairs collide and fixed-fixed, kinematic-kinematic and kinematic-fixed pairs do not.",
          "retrieved": true
        },
        {
          "title": "Colliders (User Guide: Rust 0.36)",
          "url": "https://rapier.rs/docs/user_guides/rust/colliders/",
          "year": 2026,
          "kind": "docs",
          "identifier": "",
          "claim": "Collision detection is disabled by default between two non-dynamic bodies and is enabled with ActiveCollisionTypes::default() | ActiveCollisionTypes::KINEMATIC_FIXED.",
          "retrieved": true
        },
        {
          "title": "Rapier CHANGELOG at tag v0.35.3",
          "url": "https://github.com/dimforge/rapier/blob/v0.35.3/CHANGELOG.md",
          "year": 2026,
          "kind": "release-notes",
          "identifier": "",
          "claim": "0.30.1: kinematic rigid bodies no longer fall asleep while their velocity is non-zero, however small.",
          "retrieved": true
        }
      ],
      "background_sources": []
    },
    {
      "name": "Count Rapier 0.35.3 sleep in quanta: time_until_sleep = 32·dt sleeps after quantum 32, 33 off the origin",
      "what": "Sleep is judged on displacement: each step a dynamic body whose half per-step drift is below normalized_linear_threshold·length_unit·dt adds dt to time_since_can_sleep (any other step resets it), and its island sleeps once time_since_can_sleep ≥ time_until_sleep.",
      "how": "Configure per body: `rb.activation_mut().time_until_sleep = 32.0 * dt` (seconds, default 0.5); normalized_linear_threshold (default 0.05 u/s, negative = never sleeps); angular_threshold (default 0.5 rad/s; only collider-less bodies compare against it, bodies with colliders use only its sign plus a fixed π/2 rad/s gate); RigidBodyBuilder::can_sleep(false) = RigidBodyActivation::cannot_sleep(). With dt = 1/64 the timer counts exactly: a body at rest from its first step sleeps after quantum 32 with time_since_can_sleep = 0.5. The first check compares against sleep_prev_pose = Pose::IDENTITY, so a body inserted farther than 2·threshold·dt from the identity pose loses its first quantum (measured: at (3, 1, 0) it sleeps after 33). The test is drift·0.5 < threshold·dt, so pure translation sleeps under twice the threshold (measured: 0.08 u/s sleeps, 0.12 u/s never), and sleeping zeroes the velocity.",
      "rust_version": "rapier3d-f64 0.35.3",
      "gotchas": "The field doc says the threshold is compared against the farthest-point speed; since 0.35.0 the code uses the pose displacement and ignores the velocities of dynamic bodies (the changelog says so), so a slow drift under 0.1 u/s is frozen after time_until_sleep. The 0.35.0-beta.0 defaults (0.05 u/s, 0.5 s) replaced 0.4 u/s and 2.0 s: numbers from older posts are wrong for 0.35. Islands sleep and wake as a unit, so a body touching an awake body stays awake past its own timer. sleep_prev_pose is pub(crate) and cannot be set from outside the crate.",
      "engine_note": "rapier_law.rs `build_world` sets `activation_mut().time_until_sleep = SLEEP_QUANTA * DT` (32/64 s, exact) on dynamic bodies inserted at their record position, so a body at rest from load sleeps after quantum 33, not 32; `rebuild_snapshot` hashes `time_since_can_sleep / DT`, an exact integer while dt = 1/64. A rebuilt world starts every body at sleep_prev_pose = identity, so a restore path (T2, restore-internals) resets each off-origin body's count on its first step; this is inferred from the measured first-quantum loss, not yet run as a restore.",
      "checks": [
        {
          "label": "sleep quanta at dt = 1/64: 32 at the origin, 33 elsewhere, drift and angular thresholds",
          "edition": "2021",
          "target": "host",
          "deps": [
            "rapier3d_f64"
          ],
          "expect": "runs",
          "crate_type": "bin",
          "stdout": "at rest at the origin: (Some(32), 0.0, 0.5)\nat rest at (3, 1, 0): (Some(33), 0.0, 0.5)\ndrifting 0.08 u/s: (Some(32), 0.0, 0.5)\ndrifting 0.12 u/s: (None, 0.12, 0.0)\nspinning 0.05 rad/s, angular_threshold 0.01, collider: (Some(32), 0.0, 0.5)\nspinning 0.05 rad/s, angular_threshold 0.01, no collider: (None, 0.0, 0.0)",
          "source": "use rapier3d_f64::prelude::*;\n\n// The quantum after which the body is asleep (None: still awake after 200), its x velocity\n// and time_since_can_sleep at that point.\nfn sleep_quantum(start: Vector, linvel: Vector, angvel: Vector, angular_threshold: f64, collider: bool) -> (Option<u32>, f64, f64) {\n    let dt = 1.0 / 64.0;\n    let mut w = PhysicsWorld::new();\n    w.gravity = Vector::ZERO;\n    w.integration_parameters.dt = dt;\n    let mut rb = RigidBodyBuilder::dynamic().translation(start).linvel(linvel).angvel(angvel).build();\n    rb.activation_mut().time_until_sleep = 32.0 * dt; // SLEEP_QUANTA * DT in rapier_law.rs\n    rb.activation_mut().angular_threshold = angular_threshold;\n    let h = if collider {\n        w.insert(rb, ColliderBuilder::cuboid(0.5, 0.5, 0.5)).0\n    } else {\n        w.insert_body(rb)\n    };\n    for q in 1..=200u32 {\n        w.step();\n        let b = &w.bodies[h];\n        if b.is_sleeping() {\n            return (Some(q), b.linvel().x, b.activation().time_since_can_sleep);\n        }\n    }\n    let b = &w.bodies[h];\n    (None, b.linvel().x, b.activation().time_since_can_sleep)\n}\n\nfn main() {\n    let z = Vector::ZERO;\n    let spin = Vector::new(0.0, 0.05, 0.0);\n    println!(\"at rest at the origin: {:?}\", sleep_quantum(z, z, z, 0.5, true));\n    println!(\"at rest at (3, 1, 0): {:?}\", sleep_quantum(Vector::new(3.0, 1.0, 0.0), z, z, 0.5, true));\n    println!(\"drifting 0.08 u/s: {:?}\", sleep_quantum(z, Vector::new(0.08, 0.0, 0.0), z, 0.5, true));\n    println!(\"drifting 0.12 u/s: {:?}\", sleep_quantum(z, Vector::new(0.12, 0.0, 0.0), z, 0.5, true));\n    println!(\"spinning 0.05 rad/s, angular_threshold 0.01, collider: {:?}\", sleep_quantum(z, z, spin, 0.01, true));\n    println!(\"spinning 0.05 rad/s, angular_threshold 0.01, no collider: {:?}\", sleep_quantum(z, z, spin, 0.01, false));\n}\n"
        }
      ],
      "currency": null,
      "verify_note": null,
      "sources": [
        {
          "title": "RigidBodyActivation in rapier3d_f64::dynamics (0.35.3)",
          "url": "https://docs.rs/rapier3d-f64/0.35.3/rapier3d_f64/dynamics/struct.RigidBodyActivation.html",
          "year": 2026,
          "kind": "docs",
          "identifier": "",
          "claim": "normalized_linear_threshold defaults to 0.05 units/s (negative never sleeps), angular_threshold to 0.5 rad/s and applies only to collider-less bodies, time_until_sleep to 0.5 s; time_since_can_sleep and sleeping are public.",
          "retrieved": true
        },
        {
          "title": "rapier3d-f64 0.35.3 source: src/dynamics/rigid_body_components.rs",
          "url": "https://docs.rs/crate/rapier3d-f64/0.35.3/source/src/dynamics/rigid_body_components.rs",
          "year": 2026,
          "kind": "repo",
          "identifier": "",
          "claim": "update_energy marks a dynamic body sleep-capable when drift * 0.5 < linear_threshold * dt, adds dt to time_since_can_sleep or resets it to 0, and sleep_prev_pose is pub(crate), initialised to Pose::IDENTITY.",
          "retrieved": true
        },
        {
          "title": "Rapier CHANGELOG at tag v0.35.3",
          "url": "https://github.com/dimforge/rapier/blob/v0.35.3/CHANGELOG.md",
          "year": 2026,
          "kind": "release-notes",
          "identifier": "",
          "claim": "0.35.0-beta.0 rewrote sleeping around persistent islands and changed the defaults to 0.05 and 0.5 s (from 0.4 and 2.0); 0.35.0 judges sleep eligibility on per-step pose displacement instead of velocities.",
          "retrieved": true
        }
      ],
      "background_sources": []
    },
    {
      "name": "Warm Rapier's broad phase with one CollisionPipeline::step at load, then re-mark bodies with iter_mut",
      "what": "A hand-run BroadPhaseBvh::update whose pair events are dropped loses those pairs for good; CollisionPipeline::step registers them, but at 0.35.3 it leaves every body outside the IslandManager, so bodies must be re-marked modified before the first PhysicsPipeline step.",
      "how": "At load: `CollisionPipeline::new().step(params.prediction_distance(), &mut islands, &mut broad_phase, &mut narrow_phase, &mut bodies, &mut colliders, &(), &())`. It fills the BVH (a ray that missed before the pass hits after it) and hands its pair events to the narrow phase, but it calls handle_user_changes_to_rigid_bodies(None, …) and then clears the change flags of every modified body (after a load, all of them), so no body enters the awake island. Re-mark them: `for (_, b) in bodies.iter_mut() {}` (iter_mut pushes every body into the modified set) or bodies.get_mut(h); the next PhysicsPipeline step admits them. Measured: after the pass alone a dynamic box does not move in 10 steps; after iter_mut it falls as in a world without the pass (y 1.8999 after 10 steps in both); PhysicsWorld::wake_up(h, true) does not admit it (y stays 2.0000). Build scene queries with broad_phase.as_query_pipeline(narrow_phase.query_dispatcher(), &bodies, &colliders, filter) or PhysicsWorld::query_pipeline(): a borrowed view of the broad-phase BVH, current as of the last update or step.",
      "rust_version": "rapier3d-f64 0.35.3",
      "gotchas": "BroadPhaseBvh::update is public and 'only required to generate new events': an AddPair it has emitted is never sent again, so discarding its events removes the pair from the narrow phase permanently (measured: a box above a rotated slab falls through it, y −15.431 with 0 pairs, versus resting on it after the pipeline pass, y 0.309 with 1 pair). Clearing body flags in the collision pass is 0.35.0 behaviour ('CollisionPipeline::step now clears the rigid-bodies' modified flags'). At 0.35.3 the same pass trips `debug_assert!(rb.is_fixed() || !rb.is_enabled() || rb.ids.active_island_id != u32::MAX)` in island_manager/manager.rs:140 when a dynamic body touches a collider at load: a build with debug assertions panics (exit 101), the release profile compiles the check out (debug-assertions = false), and 0.36.0 fixes it.",
      "engine_note": "rapier_law.rs `warm_broadphase` (the E3 fix, commit 39687d9) works, but its comment credits wake_up(true): re-admission comes from `world.bodies.iter_mut()` re-marking every body, and a refactor to `world.wake_up(handle, true)` would freeze every dynamic body. T2's `solver_restore` reruns the load's collision pass on mid-run worlds where bodies touch, so a native debug test of that path panics at manager.rs:140: test it in release or through the wasm build. 0.36.0 adds PhysicsWorld::detect_collisions (collision detection without stepping), a candidate replacement for this pass.",
      "checks": [
        {
          "label": "E3: a hand-updated broad phase with dropped events lets a box fall through a rotated slab",
          "edition": "2021",
          "target": "host",
          "deps": [
            "rapier3d_f64"
          ],
          "expect": "runs",
          "crate_type": "bin",
          "stdout": "hand_update: discarded=1 y=-15.431 pairs=0\npipeline_pass: y=0.309 pairs=1",
          "source": "use rapier3d_f64::pipeline::CollisionPipeline;\nuse rapier3d_f64::prelude::*;\n\n// A box above the centre of a slab rotated 0.3 rad about z: their AABBs overlap at load.\nfn world() -> (PhysicsWorld, RigidBodyHandle, Vec<ColliderHandle>) {\n    let mut w = PhysicsWorld::new();\n    w.gravity = Vector::new(0.0, -8.0, 0.0);\n    w.integration_parameters.dt = 1.0 / 64.0;\n    let slab = RigidBodyBuilder::fixed().rotation(Vector::new(0.0, 0.0, 0.3)).build();\n    let (_, c1) = w.insert(slab, ColliderBuilder::cuboid(3.0, 0.1, 1.0));\n    let (h, c2) = w.insert(\n        RigidBodyBuilder::dynamic().translation(Vector::new(0.0, 0.6, 0.0)),\n        ColliderBuilder::cuboid(0.25, 0.25, 0.25),\n    );\n    (w, h, vec![c1, c2])\n}\n\nfn main() {\n    // The pre-E3 load: update the broad phase by hand and drop its pair events.\n    let (mut w, h, cols) = world();\n    let mut events = Vec::new();\n    let params = w.integration_parameters;\n    w.broad_phase.update(&params, &w.colliders, &w.bodies, &cols, &[], &mut events);\n    let discarded = events.len();\n    for _ in 0..128 {\n        w.step();\n    }\n    println!(\n        \"hand_update: discarded={} y={:.3} pairs={}\",\n        discarded,\n        w.bodies[h].translation().y,\n        w.narrow_phase.contact_pairs().count()\n    );\n\n    // The E3 fix: one CollisionPipeline pass, then re-mark the bodies.\n    let (mut w, h, _) = world();\n    let prediction = w.integration_parameters.prediction_distance();\n    CollisionPipeline::new().step(\n        prediction,\n        &mut w.islands,\n        &mut w.broad_phase,\n        &mut w.narrow_phase,\n        &mut w.bodies,\n        &mut w.colliders,\n        &(),\n        &(),\n    );\n    for (_, b) in w.bodies.iter_mut() {\n        if !b.is_fixed() {\n            b.wake_up(true);\n        }\n    }\n    for _ in 0..128 {\n        w.step();\n    }\n    println!(\n        \"pipeline_pass: y={:.3} pairs={}\",\n        w.bodies[h].translation().y,\n        w.narrow_phase.contact_pairs().count()\n    );\n}\n"
        },
        {
          "label": "the pass leaves bodies unadmitted: iter_mut re-admits, PhysicsWorld::wake_up does not",
          "edition": "2021",
          "target": "host",
          "deps": [
            "rapier3d_f64"
          ],
          "expect": "runs",
          "crate_type": "bin",
          "stdout": "ray before any pass hits: false\nno pass, plain steps: y=1.8999\nray after the collision pass hits: true\npass only: y=2.0000\npass + bodies.iter_mut(): y=1.8999\npass + iter_mut + wake_up(true), as warm_broadphase: y=1.8999\npass + PhysicsWorld::wake_up(h, true): y=2.0000",
          "source": "use rapier3d_f64::pipeline::CollisionPipeline;\nuse rapier3d_f64::prelude::*;\n\nfn world() -> (PhysicsWorld, RigidBodyHandle) {\n    let mut w = PhysicsWorld::new();\n    w.gravity = Vector::new(0.0, -8.0, 0.0);\n    w.integration_parameters.dt = 1.0 / 64.0;\n    w.insert(RigidBodyBuilder::fixed(), ColliderBuilder::cuboid(5.0, 0.1, 5.0));\n    let (h, _) = w.insert(\n        RigidBodyBuilder::dynamic().translation(Vector::new(0.0, 2.0, 0.0)),\n        ColliderBuilder::cuboid(0.25, 0.25, 0.25),\n    );\n    (w, h)\n}\n\nfn collision_pass(w: &mut PhysicsWorld) {\n    let prediction = w.integration_parameters.prediction_distance();\n    CollisionPipeline::new().step(\n        prediction,\n        &mut w.islands,\n        &mut w.broad_phase,\n        &mut w.narrow_phase,\n        &mut w.bodies,\n        &mut w.colliders,\n        &(),\n        &(),\n    );\n}\n\nfn ray_hits(w: &PhysicsWorld) -> bool {\n    let ray = Ray::new(Vector::new(0.0, 5.0, 0.0), Vector::new(0.0, -1.0, 0.0));\n    w.query_pipeline().cast_ray(&ray, 10.0, true).is_some()\n}\n\nfn fall(mut w: PhysicsWorld, h: RigidBodyHandle, label: &str) {\n    for _ in 0..10 {\n        w.step();\n    }\n    println!(\"{label}: y={:.4}\", w.bodies[h].translation().y);\n}\n\nfn main() {\n    let (w, h) = world();\n    println!(\"ray before any pass hits: {}\", ray_hits(&w));\n    fall(w, h, \"no pass, plain steps\");\n\n    let (mut w, h) = world();\n    collision_pass(&mut w);\n    println!(\"ray after the collision pass hits: {}\", ray_hits(&w));\n    fall(w, h, \"pass only\");\n\n    let (mut w, h) = world();\n    collision_pass(&mut w);\n    for (_, _b) in w.bodies.iter_mut() {}\n    fall(w, h, \"pass + bodies.iter_mut()\");\n\n    let (mut w, h) = world();\n    collision_pass(&mut w);\n    for (_, b) in w.bodies.iter_mut() {\n        if !b.is_fixed() {\n            b.wake_up(true);\n        }\n    }\n    fall(w, h, \"pass + iter_mut + wake_up(true), as warm_broadphase\");\n\n    let (mut w, h) = world();\n    collision_pass(&mut w);\n    w.wake_up(h, true);\n    fall(w, h, \"pass + PhysicsWorld::wake_up(h, true)\");\n}\n"
        },
        {
          "label": "the load pass with a body touching the floor panics in a debug-assertion build (exit 101)",
          "edition": "2021",
          "target": "host",
          "deps": [
            "rapier3d_f64"
          ],
          "expect": "runs",
          "crate_type": "bin",
          "stdout": "load pass, one dynamic body resting on the floor",
          "exit_code": 101,
          "source": "use rapier3d_f64::pipeline::CollisionPipeline;\nuse rapier3d_f64::prelude::*;\n\nfn main() {\n    let mut w = PhysicsWorld::new();\n    w.integration_parameters.dt = 1.0 / 64.0;\n    w.insert(RigidBodyBuilder::fixed(), ColliderBuilder::cuboid(5.0, 0.1, 5.0));\n    // A box resting on the floor at load: its bottom face on the floor's top face.\n    w.insert(\n        RigidBodyBuilder::dynamic().translation(Vector::new(0.0, 0.35, 0.0)),\n        ColliderBuilder::cuboid(0.25, 0.25, 0.25),\n    );\n    println!(\"load pass, one dynamic body resting on the floor\");\n    let prediction = w.integration_parameters.prediction_distance();\n    CollisionPipeline::new().step(\n        prediction,\n        &mut w.islands,\n        &mut w.broad_phase,\n        &mut w.narrow_phase,\n        &mut w.bodies,\n        &mut w.colliders,\n        &(),\n        &(),\n    );\n    println!(\"not reached while rapier is built with debug assertions\");\n}\n"
        }
      ],
      "currency": null,
      "verify_note": null,
      "sources": [
        {
          "title": "BroadPhaseBvh in rapier3d_f64::geometry (0.35.3)",
          "url": "https://docs.rs/rapier3d-f64/0.35.3/rapier3d_f64/geometry/struct.BroadPhaseBvh.html",
          "year": 2026,
          "kind": "docs",
          "identifier": "",
          "claim": "update outputs pair events and is only required to generate new events, never re-sending an AddPair already sent; as_query_pipeline builds a QueryPipeline from the broad phase.",
          "retrieved": true
        },
        {
          "title": "rapier3d-f64 0.35.3 source: src/pipeline/collision_pipeline.rs",
          "url": "https://docs.rs/crate/rapier3d-f64/0.35.3/source/src/pipeline/collision_pipeline.rs",
          "year": 2026,
          "kind": "repo",
          "identifier": "",
          "claim": "CollisionPipeline::step passes None as the island manager to handle_user_changes_to_rigid_bodies and ends by clearing the modified bodies' change flags.",
          "retrieved": true
        },
        {
          "title": "rapier3d-f64 0.35.3 source: src/dynamics/rigid_body_set.rs",
          "url": "https://docs.rs/crate/rapier3d-f64/0.35.3/source/src/dynamics/rigid_body_set.rs",
          "year": 2026,
          "kind": "repo",
          "identifier": "",
          "claim": "RigidBodySet::iter_mut clears the modified set and pushes every body into it; get_mut pushes the one body it returns.",
          "retrieved": true
        },
        {
          "title": "rapier3d-f64 0.35.3 source: src/dynamics/island_manager/manager.rs",
          "url": "https://docs.rs/crate/rapier3d-f64/0.35.3/source/src/dynamics/island_manager/manager.rs",
          "year": 2026,
          "kind": "repo",
          "identifier": "",
          "claim": "interaction_changed debug-asserts under cfg(debug_assertions) that non-fixed enabled endpoints are registered, and rigid_body_updated admits a first-seen awake body to the awake island.",
          "retrieved": true
        },
        {
          "title": "Rapier CHANGELOG at tag v0.36.0",
          "url": "https://github.com/dimforge/rapier/blob/v0.36.0/CHANGELOG.md",
          "year": 2026,
          "kind": "release-notes",
          "identifier": "",
          "claim": "0.35.0 made CollisionPipeline::step clear the rigid bodies' modified flags; 0.36.0 stops it tripping a debug assertion when a moving body touches a collider and adds PhysicsWorld::detect_collisions.",
          "retrieved": true
        },
        {
          "title": "Profiles - The Cargo Book",
          "url": "https://doc.rust-lang.org/cargo/reference/profiles.html",
          "year": 2026,
          "kind": "book",
          "identifier": "",
          "claim": "debug-assertions turns cfg(debug_assertions) and debug_assert! on; it defaults to true in the dev profile and false in release.",
          "retrieved": true
        },
        {
          "title": "Scene queries (User Guide: Rust 0.36)",
          "url": "https://rapier.rs/docs/user_guides/rust/scene_queries/",
          "year": 2026,
          "kind": "docs",
          "identifier": "",
          "claim": "The QueryPipeline is a temporary object that reuses the broad-phase BVH, and scene queries use the collider positions at the end of the last timestep.",
          "retrieved": true
        }
      ],
      "background_sources": []
    },
    {
      "name": "Collect Rapier 0.35.3 collision events with a Sync EventHandler and ActiveEvents set on a collider",
      "what": "Collision and contact-force events are opt-in per collider (ActiveEvents, empty by default) and are delivered during the step to an EventHandler, which must be Sync unless the unsync-callbacks feature is on.",
      "how": "Give at least one collider of the pair `.active_events(ActiveEvents::COLLISION_EVENTS)` (or CONTACT_FORCE_EVENTS with a contact_force_event_threshold) and pass a handler as the step's events argument (PhysicsWorld::step_with_events(&(), &handler)). ChannelEventCollector::new(collision_sender, contact_force_sender) takes two std::sync::mpsc::Sender values; drain the receivers after the step. A custom handler implements handle_collision_event(&self, bodies, colliders, event: CollisionEvent, contact_pair: Option<&ContactPair>) and handle_contact_force_event(&self, dt, bodies, colliders, contact_pair, total_force_magnitude), keeping state in atomics or a Mutex because EventHandler: MaybeSync and MaybeSync = Sync. Measured: a box dropped on a floor and then lifted clear yields exactly one Started and one Stopped, through a counter or the channel collector; with ActiveEvents::empty() it yields none. Which body-type pairs produce contacts at all is ActiveCollisionTypes.",
      "rust_version": "rapier3d-f64 0.35.3",
      "gotchas": "A Cell or RefCell counter fails with E0277 ('`Cell<u32>` cannot be shared between threads safely … required for `Counter` to implement `MaybeSync`'); 0.35.0-beta.0 relaxed the bound from Send + Sync to Sync. Contact-force totals changed in the 0.35.2 patch (the step's total impulse, one substep fewer). 0.36.0 adds a required handle_soft_body_tear_event and a third ChannelEventCollector sender, so every custom handler breaks on that bump.",
      "engine_note": "rapier_law.rs passes no handler (`world.step()`), so no event reaches the hash today. T3's plan to hash collision-event order separately from body state needs ActiveEvents on the tracked colliders and a Sync collector (a Mutex<Vec<_>> or ChannelEventCollector) drained after `step()` in `integrate`; this lane measured delivery, not the order of simultaneous events, so T3 must measure that order before hashing it.",
      "checks": [
        {
          "label": "a Cell-based EventHandler is rejected: EventHandler requires MaybeSync (= Sync) at 0.35.3",
          "edition": "2021",
          "target": "host",
          "deps": [
            "rapier3d_f64"
          ],
          "expect": "compile_fail",
          "crate_type": "lib",
          "error_codes": [
            "E0277"
          ],
          "stderr_contains": [
            "MaybeSync"
          ],
          "source": "use rapier3d_f64::geometry::ContactPair;\nuse rapier3d_f64::prelude::*;\nuse std::cell::Cell;\n\nstruct Counter {\n    started: Cell<u32>,\n}\n\nimpl EventHandler for Counter {\n    fn handle_collision_event(&self, _b: &RigidBodySet, _c: &ColliderSet, event: CollisionEvent, _p: Option<&ContactPair>) {\n        if event.started() {\n            self.started.set(self.started.get() + 1);\n        }\n    }\n    fn handle_contact_force_event(&self, _dt: Real, _b: &RigidBodySet, _c: &ColliderSet, _p: &ContactPair, _f: Real) {}\n}\n"
        },
        {
          "label": "one Started and one Stopped with COLLISION_EVENTS, via atomics or the channel; none without",
          "edition": "2021",
          "target": "host",
          "deps": [
            "rapier3d_f64"
          ],
          "expect": "runs",
          "crate_type": "bin",
          "stdout": "counter: started=1 stopped=1\nchannel: [\"Started\", \"Stopped\"]\nno ActiveEvents: started=0 stopped=0",
          "source": "use rapier3d_f64::geometry::ContactPair;\nuse rapier3d_f64::prelude::*;\nuse std::sync::atomic::{AtomicU32, Ordering};\nuse std::sync::mpsc::channel;\n\n// EventHandler requires Sync (MaybeSync without `unsync-callbacks`): count with atomics.\n#[derive(Default)]\nstruct Counter {\n    started: AtomicU32,\n    stopped: AtomicU32,\n}\n\nimpl EventHandler for Counter {\n    fn handle_collision_event(&self, _b: &RigidBodySet, _c: &ColliderSet, event: CollisionEvent, _p: Option<&ContactPair>) {\n        let n = if event.started() { &self.started } else { &self.stopped };\n        n.fetch_add(1, Ordering::Relaxed);\n    }\n    fn handle_contact_force_event(&self, _dt: Real, _b: &RigidBodySet, _c: &ColliderSet, _p: &ContactPair, _f: Real) {}\n}\n\n// Drop a box on a floor, let it rest, then lift it clear: one touch starts and one stops.\nfn run(events: ActiveEvents, handler: &dyn EventHandler) {\n    let mut w = PhysicsWorld::new();\n    w.gravity = Vector::new(0.0, -8.0, 0.0);\n    w.integration_parameters.dt = 1.0 / 64.0;\n    w.insert(RigidBodyBuilder::fixed(), ColliderBuilder::cuboid(5.0, 0.1, 5.0));\n    let (h, _) = w.insert(\n        RigidBodyBuilder::dynamic().translation(Vector::new(0.0, 1.0, 0.0)),\n        ColliderBuilder::cuboid(0.25, 0.25, 0.25).active_events(events),\n    );\n    for _ in 0..64 {\n        w.step_with_events(&(), handler);\n    }\n    w.bodies[h].set_translation(Vector::new(0.0, 3.0, 0.0), true);\n    w.step_with_events(&(), handler);\n}\n\nfn main() {\n    let counter = Counter::default();\n    run(ActiveEvents::COLLISION_EVENTS, &counter);\n    println!(\n        \"counter: started={} stopped={}\",\n        counter.started.load(Ordering::Relaxed),\n        counter.stopped.load(Ordering::Relaxed)\n    );\n\n    let (tx, rx) = channel();\n    let (ftx, _frx) = channel();\n    run(ActiveEvents::COLLISION_EVENTS, &ChannelEventCollector::new(tx, ftx));\n    let kinds: Vec<&str> = rx.try_iter().map(|e| if e.started() { \"Started\" } else { \"Stopped\" }).collect();\n    println!(\"channel: {:?}\", kinds);\n\n    let quiet = Counter::default();\n    run(ActiveEvents::empty(), &quiet);\n    println!(\n        \"no ActiveEvents: started={} stopped={}\",\n        quiet.started.load(Ordering::Relaxed),\n        quiet.stopped.load(Ordering::Relaxed)\n    );\n}\n"
        }
      ],
      "currency": null,
      "verify_note": null,
      "sources": [
        {
          "title": "EventHandler in rapier3d_f64::pipeline (0.35.3)",
          "url": "https://docs.rs/rapier3d-f64/0.35.3/rapier3d_f64/pipeline/trait.EventHandler.html",
          "year": 2026,
          "kind": "docs",
          "identifier": "",
          "claim": "EventHandler: MaybeSync has two required methods, handle_collision_event and handle_contact_force_event, is implemented by () and ChannelEventCollector, and needs COLLISION_EVENTS or CONTACT_FORCE_EVENTS on at least one collider.",
          "retrieved": true
        },
        {
          "title": "rapier3d-f64 0.35.3 source: src/utils/mod.rs",
          "url": "https://docs.rs/crate/rapier3d-f64/0.35.3/source/src/utils/mod.rs",
          "year": 2026,
          "kind": "repo",
          "identifier": "",
          "claim": "MaybeSync is Sync unless the unsync-callbacks feature says otherwise.",
          "retrieved": true
        },
        {
          "title": "Colliders (User Guide: Rust 0.36)",
          "url": "https://rapier.rs/docs/user_guides/rust/colliders/",
          "year": 2026,
          "kind": "docs",
          "identifier": "",
          "claim": "Setting the ActiveEvents::COLLISION_EVENTS bit enables the collision events involving the collider.",
          "retrieved": true
        },
        {
          "title": "Rapier CHANGELOG at tag v0.36.0",
          "url": "https://github.com/dimforge/rapier/blob/v0.36.0/CHANGELOG.md",
          "year": 2026,
          "kind": "release-notes",
          "identifier": "",
          "claim": "0.35.0-beta.0 made PhysicsHooks and EventHandler require Sync instead of Send + Sync; 0.36.0 adds the required handle_soft_body_tear_event and a third sender to ChannelEventCollector::new.",
          "retrieved": true
        }
      ],
      "background_sources": []
    },
    {
      "name": "Filter Rapier pairs with collision_groups (no contact) or solver_groups (contact, no force); flag the hooks",
      "what": "collision_groups stop a pair before the narrow phase (no contacts, no events); solver_groups keep contacts and events but drop the forces; PhysicsHooks run only for pairs where a collider carries the matching ActiveHooks flag.",
      "how": "InteractionGroups::new(memberships: Group, filter: Group, test_mode: InteractionTestMode): And passes when (a.memberships & b.filter) != 0 && (b.memberships & a.filter) != 0, Or when either holds; the default is InteractionGroups::all(). Set them with ColliderBuilder::collision_groups and solver_groups. Measured with the floor in GROUP_1 and a box in GROUP_2 filtering GROUP_3: excluded by collision_groups, the box falls through with no contact and no event; excluded by solver_groups it falls identically (y −3.016) yet the contact exists and a Started event fires. Hooks: implement PhysicsHooks (filter_contact_pair(&self, &PairFilterContext) -> Option<SolverFlags>, filter_intersection_pair, modify_solver_contacts) and set `.active_hooks(ActiveHooks::FILTER_CONTACT_PAIRS)` (or FILTER_INTERSECTION_PAIR, MODIFY_SOLVER_CONTACTS); None drops the pair, Some(SolverFlags::empty()) keeps contacts without forces; the filter runs after collision groups and replaces the default non-dynamic filtering.",
      "rust_version": "rapier3d-f64 0.35.3",
      "gotchas": "The ColliderBuilder::collision_groups doc example calls InteractionGroups::new with two arguments: E0061 since 0.31 added the test mode. The guide's prose spells ActiveHooks::FILTER_CONTACT_PAIR; the constant is FILTER_CONTACT_PAIRS (E0599). A hook whose flag is not set on a collider is never called (measured: the same refusing hook leaves the box resting at 0.350). Under And both sides must accept each other, so a floor left at InteractionGroups::all() interacts with every box whose membership and filter are non-empty: design both colliders' groups together. Since 0.35.0 the broad phase itself filters by collision groups.",
      "engine_note": "rapier_law.rs sets no groups and no hooks and passes `&()`, so every pair interacts. Groups and hooks are law: a filter that reads anything outside the record (host state, time) breaks replay. If the post-Phase-2 mesh slice needs one-way surfaces or character-only walls, store the group bits in the world file and mix them into `signature`'s geometry hash with the shapes.",
      "checks": [
        {
          "label": "collision_groups remove contact and events; solver_groups keep both and remove only forces",
          "edition": "2021",
          "target": "host",
          "deps": [
            "rapier3d_f64"
          ],
          "expect": "runs",
          "crate_type": "bin",
          "stdout": "default: y=0.350 contact=true started=1\ncollision_groups: y=-3.016 contact=false started=0\nsolver_groups: y=-3.016 contact=true started=1",
          "source": "use rapier3d_f64::prelude::*;\nuse std::sync::mpsc::channel;\n\nfn run(label: &str, collision: InteractionGroups, solver: InteractionGroups) {\n    let mut w = PhysicsWorld::new();\n    w.gravity = Vector::new(0.0, -8.0, 0.0);\n    w.integration_parameters.dt = 1.0 / 64.0;\n    let floor_groups = InteractionGroups::new(Group::GROUP_1, Group::ALL, InteractionTestMode::And);\n    let (_, floor) = w.insert(\n        RigidBodyBuilder::fixed(),\n        ColliderBuilder::cuboid(5.0, 0.1, 5.0).collision_groups(floor_groups).solver_groups(floor_groups),\n    );\n    let (h, bx) = w.insert(\n        RigidBodyBuilder::dynamic().translation(Vector::new(0.0, 1.0, 0.0)),\n        ColliderBuilder::cuboid(0.25, 0.25, 0.25)\n            .collision_groups(collision)\n            .solver_groups(solver)\n            .active_events(ActiveEvents::COLLISION_EVENTS),\n    );\n    let (tx, rx) = channel();\n    let (ftx, _frx) = channel();\n    let events = ChannelEventCollector::new(tx, ftx);\n    let mut contact = false;\n    for _ in 0..64 {\n        w.step_with_events(&(), &events);\n        contact |= w.narrow_phase.contact_pair(floor, bx).is_some_and(|p| p.has_any_active_contact());\n    }\n    let started = rx.try_iter().filter(|e| e.started()).count();\n    println!(\"{label}: y={:.3} contact={} started={}\", w.bodies[h].translation().y, contact, started);\n}\n\nfn main() {\n    let all = InteractionGroups::all();\n    // Member of group 2, interacts only with group 3: the floor (group 1) fails the And test.\n    let not_floor = InteractionGroups::new(Group::GROUP_2, Group::GROUP_3, InteractionTestMode::And);\n    run(\"default\", all, all);\n    run(\"collision_groups\", not_floor, all);\n    run(\"solver_groups\", all, not_floor);\n}\n"
        },
        {
          "label": "a refusing filter_contact_pair hook acts only on colliders flagged FILTER_CONTACT_PAIRS",
          "edition": "2021",
          "target": "host",
          "deps": [
            "rapier3d_f64"
          ],
          "expect": "runs",
          "crate_type": "bin",
          "stdout": "hook flag off: y=0.350\nFILTER_CONTACT_PAIRS on: y=-3.016",
          "source": "use rapier3d_f64::prelude::*;\n\n// A hook that refuses every contact pair it is asked about.\nstruct Ghost;\nimpl PhysicsHooks for Ghost {\n    fn filter_contact_pair(&self, _ctx: &PairFilterContext) -> Option<SolverFlags> {\n        None\n    }\n}\n\nfn drop_box(hooks: ActiveHooks) -> f64 {\n    let mut w = PhysicsWorld::new();\n    w.gravity = Vector::new(0.0, -8.0, 0.0);\n    w.integration_parameters.dt = 1.0 / 64.0;\n    w.insert(RigidBodyBuilder::fixed(), ColliderBuilder::cuboid(5.0, 0.1, 5.0));\n    let (h, _) = w.insert(\n        RigidBodyBuilder::dynamic().translation(Vector::new(0.0, 1.0, 0.0)),\n        ColliderBuilder::cuboid(0.25, 0.25, 0.25).active_hooks(hooks),\n    );\n    for _ in 0..64 {\n        w.step_with_events(&Ghost, &());\n    }\n    w.bodies[h].translation().y\n}\n\nfn main() {\n    println!(\"hook flag off: y={:.3}\", drop_box(ActiveHooks::empty()));\n    println!(\"FILTER_CONTACT_PAIRS on: y={:.3}\", drop_box(ActiveHooks::FILTER_CONTACT_PAIRS));\n}\n"
        },
        {
          "label": "the guide's FILTER_CONTACT_PAIR and the doc example's 2-argument groups do not compile",
          "edition": "2021",
          "target": "host",
          "deps": [
            "rapier3d_f64"
          ],
          "expect": "compile_fail",
          "crate_type": "lib",
          "error_codes": [
            "E0599",
            "E0061"
          ],
          "source": "use rapier3d_f64::prelude::*;\n\n// The user guide's prose spells this flag in the singular.\npub fn flags() -> ActiveHooks {\n    ActiveHooks::FILTER_CONTACT_PAIR | ActiveHooks::MODIFY_SOLVER_CONTACTS\n}\n\n// The two-argument form printed in the ColliderBuilder::collision_groups doc example.\npub fn groups() -> InteractionGroups {\n    InteractionGroups::new(Group::GROUP_1, Group::GROUP_2)\n}\n"
        }
      ],
      "currency": null,
      "verify_note": null,
      "sources": [
        {
          "title": "InteractionGroups in rapier3d_f64::geometry (0.35.3)",
          "url": "https://docs.rs/rapier3d-f64/0.35.3/rapier3d_f64/geometry/struct.InteractionGroups.html",
          "year": 2026,
          "kind": "docs",
          "identifier": "",
          "claim": "new takes memberships, filter and an InteractionTestMode; And requires both membership/filter intersections to be non-zero, Or requires either.",
          "retrieved": true
        },
        {
          "title": "ColliderBuilder in rapier3d_f64::geometry (0.35.3)",
          "url": "https://docs.rs/rapier3d-f64/0.35.3/rapier3d_f64/geometry/struct.ColliderBuilder.html",
          "year": 2026,
          "kind": "docs",
          "identifier": "",
          "claim": "The collision_groups doc example calls InteractionGroups::new(Group::GROUP_1, Group::GROUP_2) with two arguments; solver_groups filters contact resolution only.",
          "retrieved": true
        },
        {
          "title": "PhysicsHooks in rapier3d_f64::pipeline (0.35.3)",
          "url": "https://docs.rs/rapier3d-f64/0.35.3/rapier3d_f64/pipeline/trait.PhysicsHooks.html",
          "year": 2026,
          "kind": "docs",
          "identifier": "",
          "claim": "filter_contact_pair is called only when a collider has ActiveHooks::FILTER_CONTACT_PAIRS, runs after collision groups, replaces the default non-dynamic filtering; None skips the pair and Some(SolverFlags::empty()) keeps contacts without forces.",
          "retrieved": true
        },
        {
          "title": "Colliders (User Guide: Rust 0.36)",
          "url": "https://rapier.rs/docs/user_guides/rust/colliders/",
          "year": 2026,
          "kind": "docs",
          "identifier": "",
          "claim": "collision_groups filter which pairs get contacts computed by the narrow phase, solver_groups which pairs get contact forces; the prose names ActiveHooks::FILTER_CONTACT_PAIR while its code uses FILTER_CONTACT_PAIRS.",
          "retrieved": true
        },
        {
          "title": "Rapier CHANGELOG at tag v0.35.3",
          "url": "https://github.com/dimforge/rapier/blob/v0.35.3/CHANGELOG.md",
          "year": 2026,
          "kind": "release-notes",
          "identifier": "",
          "claim": "0.31 made InteractionGroups::new require an InteractionTestMode; 0.35.0 made the broad phase filter candidate pairs by collision groups.",
          "retrieved": true
        }
      ],
      "background_sources": []
    },
    {
      "name": "Pin rapier3d-f64 0.35.3 features explicitly: block-solver is a default and docs.rs documents other builds",
      "what": "rapier3d-f64 0.35.3 has 15 features; the defaults are dim3, f64, std (which enables alloc) and block-solver; it has no simd-stable, simd-nightly or simd8 feature, and docs.rs documents a parallel + serde-serialize + debug-render build.",
      "how": "Features at 0.35.3: alloc, block-solver, bytemuck, debug-disable-legitimate-fe-exceptions, debug-render, default, dev-remove-slow-accessors, dim3, enhanced-determinism (= simba/libm_force + parry3d-f64/enhanced-determinism), f64, parallel (rayon + parry3d-f64/parallel), profiler (web-time), serde-serialize, solver-bounds-checks, std, unsync-callbacks. Write the set out: `rapier3d-f64 = { version = \"=0.35.3\", default-features = false, features = [\"dim3\", \"f64\", \"std\", \"block-solver\", \"enhanced-determinism\"] }` names exactly the set the current line resolves to (measured with cargo tree on the oracle crate, which declares the same dependency line: alloc, block-solver, dim3, enhanced-determinism, f64, std), so no upstream default change can enter unseen. Exclusions: simd8 with enhanced-determinism is a compile_error! in rapier's lib.rs, and simd8 is not a rapier3d-f64 feature at all; parallel combines with enhanced-determinism since 0.35.0-beta.0; unsync-callbacks removes the thread-pool API. Check APIs against your own build: PhysicsPipeline::configure_thread_pool (E0599) and serde for IntegrationParameters (E0277) are documented on docs.rs but absent here.",
      "rust_version": "rapier3d-f64 0.35.3",
      "gotchas": "The manifest comment above block-solver says 'NOT enabled by default in 3D' and the 0.35.0-beta.0 changelog says 'on by default in 2D', yet the default list of rapier3d-f64 0.35.3 names block-solver and it gates the contact solver's paired solve (solve_pair, with a dim3 variant): trust the default list, since cross-platform reproducibility 'still requires matching numeric features (block-solver, f32 vs f64)'. simd-stable and simd-nightly were removed in 0.35.0-beta.0; SIMD is always on through wide with a scalar fallback. serde-serialize and profiler add dependencies (serde; web-time) that the build and its review must then cover. Issue #1008 (open) reports a hang with parallel + enhanced-determinism at 0.35.1: keep parallel off.",
      "engine_note": "solver/Cargo.toml declares `rapier3d-f64 = { version = \"0.35.3\", features = [\"enhanced-determinism\"] }` with defaults on, and solver/FLAGS.md already records dim3, f64, std, block-solver and parallel off; spelling the list out in Cargo.toml turns that sentence into a pin, and a CI snapshot of `cargo tree -e features -i rapier3d-f64` (ci-reproducible-builds) catches drift. Restore work that uses serde (request 4 in docs/rust-kb-requests.md) is a feature change and a new binary.",
      "checks": [
        {
          "label": "the linked crate reports rapier3d_f64::VERSION = 0.35.3",
          "edition": "2021",
          "target": "host",
          "deps": [
            "rapier3d_f64"
          ],
          "expect": "runs",
          "crate_type": "bin",
          "stdout": "0.35.3",
          "source": "fn main() {\n    println!(\"{}\", rapier3d_f64::VERSION);\n}\n"
        },
        {
          "label": "docs.rs shows configure_thread_pool (parallel build); the pinned build lacks it: E0599",
          "edition": "2021",
          "target": "host",
          "deps": [
            "rapier3d_f64"
          ],
          "expect": "compile_fail",
          "crate_type": "lib",
          "error_codes": [
            "E0599"
          ],
          "source": "use rapier3d_f64::prelude::*;\n\n// Documented on docs.rs, which builds rapier3d-f64 with `parallel`; absent without it.\npub fn pool(p: &mut PhysicsPipeline) {\n    let _ = p.configure_thread_pool(2);\n}\n"
        },
        {
          "label": "without serde-serialize IntegrationParameters is not Serialize: E0277",
          "edition": "2021",
          "target": "host",
          "deps": [
            "rapier3d_f64",
            "serde_json"
          ],
          "expect": "compile_fail",
          "crate_type": "lib",
          "error_codes": [
            "E0277"
          ],
          "source": "use rapier3d_f64::prelude::*;\n\n// IntegrationParameters derives Serialize only under `serde-serialize`.\npub fn save(p: &IntegrationParameters) -> String {\n    serde_json::to_string(p).unwrap()\n}\n"
        }
      ],
      "currency": null,
      "verify_note": null,
      "sources": [
        {
          "title": "rapier3d-f64 0.35.3 feature flags (docs.rs)",
          "url": "https://docs.rs/crate/rapier3d-f64/0.35.3/features",
          "year": 2026,
          "kind": "docs",
          "identifier": "",
          "claim": "Lists the 15 features of 0.35.3 with block-solver, dim3, f64, std and alloc as defaults and enhanced-determinism enabling parry3d-f64/enhanced-determinism and simba/libm_force.",
          "retrieved": true
        },
        {
          "title": "rapier3d-f64 0.35.3 source: Cargo.toml.orig",
          "url": "https://docs.rs/crate/rapier3d-f64/0.35.3/source/Cargo.toml.orig",
          "year": 2026,
          "kind": "repo",
          "identifier": "",
          "claim": "default = [dim3, f64, std, block-solver] while the comment above block-solver says it is not enabled by default in 3D; unsync-callbacks drops the Sync requirement and the thread-pool API.",
          "retrieved": true
        },
        {
          "title": "rapier3d-f64 0.35.3 source: src/lib.rs",
          "url": "https://docs.rs/crate/rapier3d-f64/0.35.3/source/src/lib.rs",
          "year": 2026,
          "kind": "repo",
          "identifier": "",
          "claim": "A compile_error! forbids simd8 together with enhanced-determinism because it breaks cross-platform determinism.",
          "retrieved": true
        },
        {
          "title": "rapier3d-f64 0.35.3 source: src/dynamics/solver/contact_constraint/contact_constraint_element.rs",
          "url": "https://docs.rs/crate/rapier3d-f64/0.35.3/source/src/dynamics/solver/contact_constraint/contact_constraint_element.rs",
          "year": 2026,
          "kind": "repo",
          "identifier": "",
          "claim": "cfg(feature = \"block-solver\") gates the r_mat_elts fields, solve_mlcp_two_constraints and solve_pair, including a cfg(all(feature = \"dim3\", feature = \"block-solver\")) solve_pair.",
          "retrieved": true
        },
        {
          "title": "Rapier CHANGELOG at tag v0.35.3",
          "url": "https://github.com/dimforge/rapier/blob/v0.35.3/CHANGELOG.md",
          "year": 2026,
          "kind": "release-notes",
          "identifier": "",
          "claim": "0.35.0-beta.0 removed simd-stable/simd-nightly, made enhanced-determinism combinable with parallel, kept it incompatible with simd8 and said cross-platform reproducibility requires matching numeric features (block-solver, f32 vs f64).",
          "retrieved": true
        },
        {
          "title": "PhysicsPipeline::step hangs in join_deferred_bvh_optimize with parallel enabled (issue #1008)",
          "url": "https://github.com/dimforge/rapier/issues/1008",
          "year": 2026,
          "kind": "issue",
          "identifier": "dimforge/rapier#1008",
          "claim": "An open report of a step hang with rapier3d 0.35.1 built with parallel and enhanced-determinism on a two-worker pool.",
          "retrieved": true
        }
      ],
      "background_sources": []
    },
    {
      "name": "Hold Rapier to its stated determinism terms: same build and inputs, enhanced-determinism, same insertion order",
      "what": "Rapier promises identical reruns on one machine with the same Rapier and Rust versions and the same initial conditions, and cross-platform identity only with enhanced-determinism, IEEE 754-2008 targets and identical insertion order; nothing is promised across Rapier versions.",
      "how": "The crate root at 0.35.3 promises 'a perfectly deterministic simulation on different machine, as long as they are compliant with the IEEE 754-2008 floating point standard'. The user guide (labelled Rust 0.36) makes local determinism conditional on the same initial conditions, the same version of Rapier and the same version of the Rust compiler, and cross-platform determinism additionally on enhanced-determinism (not with simd8; parallel allowed, identical to the sequential solver), strict IEEE 754-2008 targets, initial values computed with float functions taken from nalgebra's ComplexField/RealField rather than std, and structures initialised with the same values and items added in the exact same order. The 0.35.0-beta.0 changelog adds that cross-platform determinism rests on glam's scalar core plus libm-pinned transcendentals and requires matching block-solver and f32/f64. In the code, enhanced-determinism switches the step's joint wake-up and join drains from drain() to drain(..), makes parry's HashMap an IndexMap, and canonicalises signed zeros in stored contact impulses (0.35.0). Measured: a four-box pile built twice in the same order is bit-identical after 200 steps; built in reverse insertion order it is not.",
      "rust_version": "rapier3d-f64 0.35.3",
      "gotchas": "No release promises identical results across versions, and the 0.35.x patches changed behaviour (0.35.2: reported contact impulses, multibody joints), so the exact version pin is part of the determinism contract. Insertion order is state: it decides the handles and, as measured, the result bits, so a restore or a host binding that rebuilds bodies in another order diverges. The guide's cross-platform list does not mention numeric features; the block-solver / f64 condition appears only in the changelog.",
      "engine_note": "si-rpg-engine meets the written terms: enhanced-determinism on, parallel off, f64, block-solver by default, rustc 1.98.1 pinned by solver/rust-toolchain.toml, and `build_world` inserts slabs, the heightfield, then bodies in record order. Rapier's promise covers IEEE 754 CPUs; the three-JS-engine golden is the engine's own evidence for wasm engines. T3's test that insertion order changes the hash should go red, as it does here on a box pile.",
      "checks": [
        {
          "label": "same insertion order: bit-identical after 200 steps; reversed insertion order: different",
          "edition": "2021",
          "target": "host",
          "deps": [
            "rapier3d_f64"
          ],
          "expect": "runs",
          "crate_type": "bin",
          "stdout": "same order twice identical: true\nreversed insertion order identical: false",
          "source": "use rapier3d_f64::prelude::*;\n\n// FNV-1a over the final bits of four boxes, read in scene order (not handle order).\nfn pile(order: &[usize]) -> u64 {\n    let starts = [\n        Vector::new(0.00, 0.40, 0.00),\n        Vector::new(0.13, 0.95, 0.07),\n        Vector::new(-0.11, 1.50, 0.05),\n        Vector::new(0.05, 2.05, -0.12),\n    ];\n    let mut w = PhysicsWorld::new();\n    w.gravity = Vector::new(0.0, -8.0, 0.0);\n    w.integration_parameters.dt = 1.0 / 64.0;\n    w.insert(RigidBodyBuilder::fixed(), ColliderBuilder::cuboid(5.0, 0.1, 5.0));\n    let mut handles = [RigidBodyHandle::invalid(); 4];\n    for &i in order {\n        let (h, _) = w.insert(RigidBodyBuilder::dynamic().translation(starts[i]), ColliderBuilder::cuboid(0.25, 0.25, 0.25));\n        handles[i] = h;\n    }\n    for _ in 0..200 {\n        w.step();\n    }\n    let mut h = 0xcbf29ce484222325u64;\n    for hd in handles {\n        let b = &w.bodies[hd];\n        let (p, q) = (b.translation(), b.rotation());\n        for x in [p.x, p.y, p.z, q.x, q.y, q.z, q.w] {\n            for byte in x.to_bits().to_le_bytes() {\n                h = (h ^ byte as u64).wrapping_mul(0x100000001b3);\n            }\n        }\n    }\n    h\n}\n\nfn main() {\n    let a = pile(&[0, 1, 2, 3]);\n    println!(\"same order twice identical: {}\", a == pile(&[0, 1, 2, 3]));\n    println!(\"reversed insertion order identical: {}\", a == pile(&[3, 2, 1, 0]));\n}\n"
        }
      ],
      "currency": null,
      "verify_note": null,
      "sources": [
        {
          "title": "rapier3d_f64 crate documentation (0.35.3)",
          "url": "https://docs.rs/rapier3d-f64/0.35.3/rapier3d_f64/",
          "year": 2026,
          "kind": "docs",
          "identifier": "",
          "claim": "Rapier claims snapshot/restore and a perfectly deterministic simulation on different machines as long as they comply with IEEE 754-2008.",
          "retrieved": true
        },
        {
          "title": "Determinism (User Guide: Rust 0.36)",
          "url": "https://rapier.rs/docs/user_guides/rust/determinism",
          "year": 2026,
          "kind": "docs",
          "identifier": "",
          "claim": "Local determinism needs the same initial conditions, Rapier version and Rust compiler; cross-platform determinism also needs enhanced-determinism (not with simd8), strict IEEE 754-2008 targets and items added in the exact same order.",
          "retrieved": true
        },
        {
          "title": "Rapier CHANGELOG at tag v0.35.3",
          "url": "https://github.com/dimforge/rapier/blob/v0.35.3/CHANGELOG.md",
          "year": 2026,
          "kind": "release-notes",
          "identifier": "",
          "claim": "0.35.0-beta.0 bases cross-platform determinism on glam's scalar core plus libm-pinned transcendentals and requires matching block-solver and f32/f64; 0.35.0 canonicalises signed zeros in stored contact impulses; 0.35.2 changed reported impulses and multibody joints.",
          "retrieved": true
        },
        {
          "title": "rapier3d-f64 0.35.3 source: src/pipeline/physics_pipeline/substep.rs",
          "url": "https://docs.rs/crate/rapier3d-f64/0.35.3/source/src/pipeline/physics_pipeline/substep.rs",
          "year": 2026,
          "kind": "repo",
          "identifier": "",
          "claim": "Under enhanced-determinism the step drains the joints' to_wake_up and to_join sets with drain(..) instead of drain().",
          "retrieved": true
        },
        {
          "title": "rapier3d-f64 0.35.3 source: src/utils/mod.rs",
          "url": "https://docs.rs/crate/rapier3d-f64/0.35.3/source/src/utils/mod.rs",
          "year": 2026,
          "kind": "repo",
          "identifier": "",
          "claim": "parry's HashMap is an IndexMap under enhanced-determinism and a hashbrown map otherwise; canonicalize_zero keeps -0.0 out of serialized solver state.",
          "retrieved": true
        }
      ],
      "background_sources": []
    },
    {
      "name": "Govern a rapier3d-f64 bump: pin =0.35.3, read every CHANGELOG entry to the target, rerun course and goldens",
      "what": "Most Rapier releases from 0.30 to 0.36.0 changed simulation behaviour, patch releases included (0.30.1, 0.35.2, 0.35.3), and 0.36.0 (on crates.io since 2026-09-25) breaks the API this engine calls, so a bump is a law change gated by the outcome course and the goldens.",
      "how": "1) Manifest: \"0.35.3\" is a caret requirement, >=0.35.3, <0.36.0 (the Cargo book's table: ^0.2.3 := >=0.2.3, <0.3.0), so `cargo update` may take a 0.35.4; write \"=0.35.3\" so only a reviewed commit moves it. 2) Read the CHANGELOG from the pinned tag to the target and list every entry that can move numbers. 0.31: InteractionGroups test mode, contact_softness replaces the erp/frequency knobs. 0.32: math moved to glam (Pose, by-value getters). 0.33: PhysicsWorld; CCD consults filter_contact_pair. 0.35.0-beta.0: persistent-island sleep with new thresholds, a single active set, the sweep-based CCD with automatic sweeps against fixed colliders, velocity caps, new contact defaults, static_contact_softness, the staged solver, FTZ/DAZ flag removed, clustering and recycling on. 0.35.0: displacement-based sleep, restitution as an end-of-step pass, broad-phase filtering by groups and of same-body pairs, canonical signed-zero impulses. 0.35.2: contact impulses, multibody compliance. 0.36.0: step takes a SoftBodySet, EventHandler gains a required method, ContactPair::manifolds and solver_clusters move to ContactPair::contacts, earlier snapshots no longer load, parry 0.31, CCD and dt == 0 fixes, PhysicsWorld::detect_collisions. 3) On the bump branch run T4's course and outcome tests first, then the goldens under the three engines, T1's behaviour numbers, T2's restore round trip and the Linux digest; move each golden once, in one commit that names the changelog entries. 4) Read version-exact docs: rapier.rs is 'User Guide: Rust (0.36)' with no Rust version selector, so for 0.35.3 use docs.rs/rapier3d-f64/0.35.3 and the v0.35.3 tag.",
      "rust_version": "rapier3d-f64 0.35.3 → 0.36.0",
      "gotchas": "The rapier.rs guide already shows 0.36 code, and docs.rs 0.35.3 still carries pre-0.35 defaults in two doc comments and is built with parallel, serde-serialize and debug-render. The Cargo.lock pin holds only until someone runs `cargo update`. A bump that moves results must move the goldens; the course is what shows the new numbers still honour the limits, which is why T4 pin 6 runs it before any golden is rewritten. 0.36.0 is not a drop-in: per its changelog the ContactPair fields that solver_clear_warmstart writes have moved. This lane compared no 0.36 simulation numbers.",
      "engine_note": "solver/Cargo.toml has `version = \"0.35.3\"` (caret) with solver/Cargo.lock at 0.35.3; T4 pin 6 adds FLAGS.md's sentence that any rapier3d-f64 or toolchain bump reruns harness/course.test.js before a golden moves. A 0.36 bump rewrites rapier_law.rs `solver_clear_warmstart` (it writes pair.manifolds and pair.solver_clusters, which move to ContactPair::contacts), must recheck `rebuild_snapshot`'s pair.solver_manifolds() against 0.36's accessors, and can replace `warm_broadphase` with PhysicsWorld::detect_collisions.",
      "checks": [
        {
          "label": "tripwire: the 0.35.3 surface rapier_law.rs uses that 0.36.0 changes compiles today",
          "edition": "2021",
          "target": "host",
          "deps": [
            "rapier3d_f64"
          ],
          "expect": "compiles",
          "crate_type": "lib",
          "source": "use rapier3d_f64::geometry::{ContactManifold, ContactPair};\nuse rapier3d_f64::prelude::*;\nuse std::sync::mpsc::Sender;\n\n// rebuild_snapshot and solver_clear_warmstart read and write these pub fields;\n// 0.36.0 moves `manifolds` and `solver_clusters` into ContactPair::contacts.\npub fn warmstart_sources(pair: &mut ContactPair) -> (usize, &mut Vec<ContactManifold>) {\n    let n = pair.solver_clusters.len() + pair.solver_manifolds().len();\n    (n, &mut pair.manifolds)\n}\n\n// A handler with exactly the two 0.35.3 methods; 0.36.0 adds a required third.\npub struct Quiet;\nimpl EventHandler for Quiet {\n    fn handle_collision_event(&self, _: &RigidBodySet, _: &ColliderSet, _: CollisionEvent, _: Option<&ContactPair>) {}\n    fn handle_contact_force_event(&self, _: Real, _: &RigidBodySet, _: &ColliderSet, _: &ContactPair, _: Real) {}\n}\n\n// Two senders at 0.35.3; 0.36.0 takes a third for soft-body tear events.\npub fn collector(c: Sender<CollisionEvent>, f: Sender<ContactForceEvent>) -> ChannelEventCollector {\n    ChannelEventCollector::new(c, f)\n}\n\n// The 12-argument step (0.36.0 inserts a SoftBodySet).\npub fn step(w: &mut PhysicsWorld) {\n    w.physics_pipeline.step(\n        w.gravity,\n        &w.integration_parameters,\n        &mut w.islands,\n        &mut w.broad_phase,\n        &mut w.narrow_phase,\n        &mut w.bodies,\n        &mut w.colliders,\n        &mut w.impulse_joints,\n        &mut w.multibody_joints,\n        &mut w.ccd_solver,\n        &(),\n        &(),\n    );\n}\n"
        }
      ],
      "currency": null,
      "verify_note": null,
      "sources": [
        {
          "title": "Rapier CHANGELOG at tag v0.36.0",
          "url": "https://github.com/dimforge/rapier/blob/v0.36.0/CHANGELOG.md",
          "year": 2026,
          "kind": "release-notes",
          "identifier": "",
          "claim": "0.36.0 (24 September 2026) makes step take the SoftBodySet, adds a required EventHandler method, moves ContactPair::manifolds and solver_clusters to ContactPair::contacts and cannot load snapshots from previous versions; the file records every 0.30-0.35.3 behaviour change listed here.",
          "retrieved": true
        },
        {
          "title": "Rapier CHANGELOG at tag v0.35.3",
          "url": "https://github.com/dimforge/rapier/blob/v0.35.3/CHANGELOG.md",
          "year": 2026,
          "kind": "release-notes",
          "identifier": "",
          "claim": "0.35.3 changed multibody frictionloss and the 0.35.2 patch changed reported contact impulses and multibody joint compliance.",
          "retrieved": true
        },
        {
          "title": "Specifying Dependencies - The Cargo Book",
          "url": "https://doc.rust-lang.org/cargo/reference/specifying-dependencies.html",
          "year": 2026,
          "kind": "book",
          "identifier": "",
          "claim": "A bare version string is a caret requirement and ^0.2.3 allows >=0.2.3, <0.3.0; = gives an exact comparison requirement.",
          "retrieved": true
        },
        {
          "title": "rapier3d-f64 versions (crates.io API)",
          "url": "https://crates.io/api/v1/crates/rapier3d-f64/versions",
          "year": 2026,
          "kind": "repo",
          "identifier": "",
          "claim": "rapier3d-f64 0.36.0 was published 2026-09-25 and 0.35.3 on 2026-08-28, with 0.35.0, 0.35.1 and 0.35.2 in August 2026.",
          "retrieved": true
        },
        {
          "title": "Simulation structures (User Guide: Rust 0.36)",
          "url": "https://rapier.rs/docs/user_guides/rust/simulation_structures/",
          "year": 2026,
          "kind": "docs",
          "identifier": "",
          "claim": "The rapier.rs guide is labelled Rust 0.36 and shows the 0.36 step signature with a SoftBodySet.",
          "retrieved": true
        }
      ],
      "background_sources": []
    }
  ]
}
